
    private %upNameContext %name(%args) throws %classnameLexer.ParseException {
        %upNameContext result = new %upNameContext();
%initCode
        switch (lex.curToken().type) {
%cases
            default:
                throw new %classnameLexer.ParseException("Invalid token " + lex.curToken().type + ". Token types [ %expectedTokens ] expected", lex.lastPos());
        }
    }
